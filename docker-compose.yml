services:
  web:
    build: .
    container_name: web-v
    command: >
      sh -c "python manage.py collectstatic --noinput &&
         python manage.py makemigrations &&
         python manage.py migrate &&
         python manage.py update_translation_fields &&
         python manage.py compilemessages &&
         gunicorn config.asgi:application -k uvicorn.workers.UvicornWorker --bind 0.0.0.0:8000 --workers 4"
    volumes:
      - .:/app
    expose:
      - "8000"
    depends_on:
      - db
    environment:
      - DJANGO_SETTINGS_MODULE=config.settings
    env_file:
      - .env
    restart: unless-stopped

  nginx:
    image: nginx:stable
    container_name: nginx-v
    ports:
      - "8000:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
      - ./staticfiles:/app/staticfiles
      - ./media:/app/media
    depends_on:
      - web
      - db
    restart: unless-stopped

  db:
    image: postgres:15
    container_name: db
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${DB_NAME}
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"

      # redis:
      #   image: redis:7
      #   container_name: redis-i
      #   ports:
      #     - "6379:6379"
      # - db

      # celery_worker:
      #   build: .
      #   container_name: celery_worker-i
      #   user: celeryuser
      #   command: celery -A config worker -l info --pool=solo
      #   volumes:
      #     - .:/app
      #   working_dir: /app
      #   depends_on:
      #     - redis
      #     - web
      #   env_file:
      #     - .env
      #   restart: always

      # celery_beat:
      #   build: .
      #   container_name: celery_beat-i
      #   user: celeryuser
      #   command: celery -A config beat -l info --schedule=/tmp/celerybeat-schedule
      #   volumes:
      #     - .:/app
      #   working_dir: /app
      #   depends_on:
      #     - redis
      #     - web
      #   env_file:
      #     - .env
      #   restart: always
volumes:
  postgres_data:
